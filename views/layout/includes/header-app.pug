header.bg-indigo-600.p-4
            .container.mx-auto.flex.justify-between.items-center
                a(href="/")
                    h1.text-2xl.text-white.font-extrabold.text-center Bienes
                        span.font-normal Raices

                nav(class="my-2 text-sm md:flex md:items-center font-bold text-white md:gap-4")

                    if !sesion
                        a(href="/auth/registro") Crear Cuenta
                        a(href="/auth/login") Iniciar Sesion
                    else
                        a(href="/mis-propiedades") Mis Propiedades
                        a(href="/") Mi Perfil
                        form(method="POST" action="/auth/cerrar-sesion" )
                            input(type="hidden" name="_csrf" value=csrfToken )
                            input(type="submit" class="bg-indigo-800 py-2 px-10 rounded-lg cursor-pointer"  value="Cerrar Sesion")

div(class="bg-indigo-700 py-5 hidden lg:block")
    div(class="container mx-auto flex justify-between px-10")
        nav(class="flex gap-4 items-center")
            a(href="/categorias/1" class="text-sm font-bold uppercase text-white") Casas
            a(href="/categorias/2" class="text-sm font-bold uppercase text-white") Departamentos
            a(href="/categorias/3" class="text-sm font-bold uppercase text-white") Bodegas
            a(href="/categorias/4" class="text-sm font-bold uppercase text-white") Terrenos
            a(href="/categorias/5" class="text-sm font-bold uppercase text-white") Cabañas

        form(action="/buscador" method="POST" class="flex gap-3 items-center")
            input(type="hidden" name="_csrf" value=csrfToken)
            label(for="termino" class="text-sm uppercase font-bold text-white") Busqueda 
            input(type="text" id="termino" name="termino" placeholder="Buscar Propiedades" class="p-2 rounded-lg")

            input(type="submit" class="bg-indigo-800 rounded-lg text-white uppercase font-bold p-2 hover:bg-indigo-400 cursor-pointer text-sm" value="buscar")
