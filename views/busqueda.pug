extends ./layout/app

block contenido
    section(class="py-5 px-5")
        h2(class="text-2xl uppercase text-gray-800 font-bold text-center italic")= pagina 

        if propiedades.length
            div(class="grid grid-cols-1 md:grid-cols-3 gap-5 mt-14")
                each propiedad in propiedades
                    include ./templates/card
        else 
            p(class="text-center text-gray-800 text-sm py-10") No Hay Resultados